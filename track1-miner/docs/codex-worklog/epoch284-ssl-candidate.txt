api.example.com is unreachable, so its TLS/SSL certificate configuration cannot be analyzed currently. Certificate chain completeness and hostname validation cannot be verified. When reachable, run openssl s_client -connect api.example.com:443 -showcerts. Verify the server presents leaf and intermediate certificates to build a complete trust path. Inspect Subject Alternative Name and confirm DNS:api.example.com. Use SSL Labs Server Test to confirm certificate chain, hostname validation, and overall grade.
