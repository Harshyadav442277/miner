# Recovery for miners ranked below third

Live baseline read 2026-09-13 16:45 UTC: registration **1408**, catalog **4433**, owner
`0xdad201ef02f5c1fbb8f9e931ae9b7c1bf493a39e`, all 36 intents scored in epoch **329**.
Sixteen rank first, including two all-zero ties. Twelve rank fourth or lower.
Identity and registered manifest hash were checked together; the manifest is unchanged.

| Intent | Baseline rank | Diagnosis / action |
|---|---:|---|
| WEATHER_FORECAST | 4 | Reproduced forecasts for Lagos/Abuja incorrectly resolving Return, South Carolina. Fixed candidate ordering and instruction-word handling. |
| WEATHER_CHECK | 12 | The zero is a Telegraph request-builder LLM timeout before the endpoint call. Separately reproduced and fixed the same wrong-city bug in current conditions. |
| FRAUD_DETECTION | 6 | Hash-only requests reported no indicators without checking anything. Now resolve sender/recipient and screen sanctions; absent evidence stays unknown. Negated credential warnings no longer trigger a disclosure marker. |
| WEB_SEARCH | 8 | MK-2870 returned only a definition; product-code retry recovers dated reporting. Reject unrelated company background and code-prefix collisions. Old reporting is labelled available, not current. |
| URL_SCAN | 4 | Healthy and known-phishing live probes behave correctly. Its 0.83959 vs 0.99531 gap does not establish a defect in the hidden question. |
| ONCHAIN_TX_LOOKUP | 6 | Existing receipt/address routes respond. The 0.01203 vs 0.99497 gap remains unexplained without scorer question/ground truth. Existing receipt wording preserved. |
| TVL_LOOKUP | 4 | Routed sample contains missing subjects, misroutes, and USDC without a contract. No unsupported protocol or liquidity figure substituted. |
| WALLET_BALANCE_CHECK | 6 | Sample balances respond; some routed questions ask for contract activity instead. Both latest leader and ours are near zero. |
| CURRENCY_EXCHANGE | 4 | USD/INR and EUR queries return dated reference rates. Latest scores are near zero. |
| CRYPTO_PRICE | 12 | BTC, ETH, DOGE, USDC direct probes return prices. No recorded failure reason; even the leader scores only 3.6e-30. Zero is not proved to be a runtime outage. |
| STOCK_PRICE | 6 | Listed-company query returns a dated price; ambiguous/unlisted companies are not replaced by similarly named equities. Latest field scores are near zero. |
| RESEARCH_SYNTHESIS | 4 | Supplied-note attribution retained; speculative trial questions with no quotable study stay unresolved. No invented findings. |

The rank audit's stale default registration was updated to 1408. Future overrides remain available
through `REGISTRATION_ID`; identity/activation/manifest checks still fail closed.

## Verification

- Full suite: **704/704**, including live-provider tests.
- Final fraud/web changes: **29/29** focused tests, including live sources.
- Nine explicit release checks all fail against the previous production build. They cover four
  wrong-city cases, empty fraud evidence, negated advice, transaction-party screening, code retrieval,
  and unrelated company background.
- Preview and production release evidence is recorded under
  `evidence/rank-below3-2026-09-13/`.
- Preview `miner-3vmtcv4s6`: **9/9** release checks. Promotion rebuilt it as production
  `miner-1yltvl5d8`; both aliases verified by Vercel inspection. Immediately after promotion the
  first two checks still received old weather answers; an unchanged rerun passed **9/9**
  (`production-check-confirm.json`). This is consistent with alias propagation; the initial
  failures are retained rather than omitted.
- Registration 1408 remains active. The pre-release rollback is `miner-ohia88jfz`.

These measurements establish answer correctness and retrieval improvements, **not improved ranks**.
The baseline epoch preceded this release. Re-read a subsequent complete authoritative epoch and
its failure reasons before attributing a rank movement to these changes. Public score rows do not
contain the hidden questions, converted answers or ground truths, so no hidden-score reproduction
is claimed.

```powershell
node track1-miner/tools/rank-audit.mjs track1-miner/docs/evidence/rank-below3-2026-09-13/next-epoch
node track1-miner/tools/below3-release.mjs
node track1-miner/tools/watch.mjs --registration-id 1408 --base-url https://miner-wine.vercel.app --once
```
